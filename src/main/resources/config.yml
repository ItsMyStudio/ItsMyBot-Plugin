websocket:
  host: "localhost" # WebSocket server host
  port: 8765 # WebSocket server port
  server_id: "mc-server-1" # Unique identifier for the server
  jwt_secret: "#### REPLACE WITH YOUR SECRET ####" # Secret key for JWT signing

# Prefix for chat messages
prefix: "<gray>[<aqua>ItsMyBot</aqua>]</gray> "